import {colors} from "@/themes";

export const COMMON_RA_SELECT_INPUT_SX_PROPS = {
  "& [role='combobox']": {
    padding: "5.5px 14px",
  },
};

export const COMMON_PAPER_BOX_SX = {
  bgcolor: "#fff",
  border: `1px solid ${colors("gray-0")}`,
} as const;
