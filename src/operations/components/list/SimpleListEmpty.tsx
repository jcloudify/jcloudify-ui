import {COMMON_PAPER_BOX_SX} from "@/components/constants";
import {Stack} from "@mui/material";

export const SimpleListEmpty = () => (
  <Stack sx={COMMON_PAPER_BOX_SX} p={2}>
    No results
  </Stack>
);
